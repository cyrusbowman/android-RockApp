<?xml version="1.0" encoding="utf-8"?>
<!-- Layout has a transparent background color to indicate to the user they are doing something odd (moving the rock) -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:visibility="invisible"
    android:background="@color/rock_move_background" >
    
    <!-- This view gets the drawable of the rock being move and is what gets moved around -->
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher" />
    
</FrameLayout>