<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Accept the new position of the rock -->
    <item 
        android:id="@+id/accept" 
        android:showAsAction="always|withText" 
        android:menuCategory="container" 
        android:icon="@drawable/accept"
        android:title="@string/menu_rock_edit_accept"
        android:visible="true"
        android:enabled="true" />
    
    <!-- Reject the new position of the rock -->
    <item 
        android:id="@+id/reject"
        android:showAsAction="always|withText" 
        android:menuCategory="container" 
        android:icon="@drawable/reject"
        android:title="@string/menu_rock_edit_reject"
        android:visible="true"
        android:enabled="true" />
</menu>	